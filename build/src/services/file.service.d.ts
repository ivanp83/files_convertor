import { AppTypes } from '@shared/types';
import { NextFunction } from 'express';
declare const _default: {
    convertFile: (file: AppTypes.FileType, format: string, next: NextFunction) => Promise<any>;
};
export = _default;
